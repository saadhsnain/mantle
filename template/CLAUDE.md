# Claude

Mantle harness. `AGENTS.md` is canonical — read it first.

Repo docs: `README.md`, `CHANGELOG.md`, `DECISIONS.md`. Skills: `skills/`.

If available, stack defaults inherit from `~/.claude/CLAUDE.md`.
