# __PROJECT_NAME__

Project scaffolded from Mantle.

## Setup

Edit this section with the project's real setup commands.

## Run

Edit this section with the project's real run commands.

## Architecture

Edit this section with the project's real architecture, overview, and glossary. This README is the synthesized current-state doc — keep it accurate as the project evolves.

## Project Notes

- `AGENTS.md` is the canonical agent guide.
- This `README.md` holds current project truth; `DECISIONS.md` records *why* (append-only, ADR-style).
- `scripts/log-change.sh` records material changes in `CHANGELOG.md`.
